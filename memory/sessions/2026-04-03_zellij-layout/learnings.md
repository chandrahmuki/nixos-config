---
Generated: 2026-04-03 11:06 UTC
---

# Learnings

- `zellij action dump-layout` (ou `--session <nom>`) = meilleur moyen d'obtenir les proportions exactes
- Zellij supporte bien le drag & drop de panes (feature récente, pas dans ma knowledge)
- `mouse_mode = true` déjà actif dans les settings — pas besoin de l'ajouter
- La sérialisation Zellij persiste les tabs dynamiques pendant la session mais pas au reboot
- Le MCP NixOS n'était pas disponible dans cette session (pas de tool deferred trouvé)
- index_sessions.md n'est PAS trié par date de création — toujours utiliser `ls -lt` sur le dossier sessions/
